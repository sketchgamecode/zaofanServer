export const serverGlobalConfig = {
  /**
   * 酒馆经验测试倍率
   * 默认为 1。可使用 /api/debug/config 接口动态调整。
   */
  debugTavernXpMultiplier: 1,

  /**
   * 酒馆金币测试倍率
   * 默认为 1。可使用 /api/debug/config 接口动态调整。
   */
  debugTavernCopperMultiplier: 1,
};
